import pandas

class EconVariable(object):
	def __init__(self,dlstats_DataFrame):
		self.

class GDP(object):
	def __init__(self,supply,demand):
		self.supply = supply
		self.demand = demand

class Country(Object):
	def __init__(self,name):
		self.name = name
		
		import self.name

	#This should be an object (indicator) that supports components, forecasts
	@property
	def GDP(self):
		return self._GDP
	GDP = property(get_GDP,set_GDP,doc="Gross Domestic Product")

	

